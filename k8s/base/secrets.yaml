apiVersion: v1
kind: Secret
metadata:
  name: klline-secrets
  namespace: klline
type: Opaque
stringData:
  postgres-password: "klline"
  secret-key: "changeme-set-in-overlay"
  minio-access-key: "minioadmin"
  minio-secret-key: "minioadmin"
