apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: klline
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      initContainers:
        - name: wait-for-postgres
          image: image-registry.openshift-image-registry.svc:5000/klline/klline-backend:latest
          command:
            - sh
            - -c
            - |
              until pg_isready -h postgres -U $POSTGRES_USER; do
                echo "Waiting for postgres..."; sleep 2
              done
          envFrom:
            - secretRef:
                name: klline-secrets
            - configMapRef:
                name: klline-config
      containers:
        - name: backend
          image: image-registry.openshift-image-registry.svc:5000/klline/klline-backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: klline-secrets
            - configMapRef:
                name: klline-config
          command:
            - uvicorn
            - app.main:app
            - --host
            - "0.0.0.0"
            - --port
            - "8000"
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
